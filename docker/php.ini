; Development defaults. PHP refuses an upload above these before the
; application sees it, so they must be at least as large as the board's own
; avatar limit or members hit a ceiling the board never mentions.
upload_max_filesize = 8M
post_max_size = 12M
memory_limit = 256M
